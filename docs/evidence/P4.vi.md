# Evidence P4 — memory tái sử dụng

[English](P4.en.md) | Tiếng Việt

## 1. Kết quả và source

**Historical snapshot: partial khi handoff ban đầu được viết.** Đã triển khai P4-S01–S07; trạng thái pending bên dưới được supersede bởi follow-up.
Base `c9bb106`; bảo toàn review commit cập nhật đồng thời
`a76b89d8f22cab8e2cca04f18f5a2175c3efa33d`. Source digest/remote CI cuối: **pending**.

### Follow-up audit — 23/09/2026

P5 đã chạy lại `Verify-Phase.ps1 -Phase P4 -Json` trên source digest
`sha256:d78de82effebe4d06a5ab47f2619ea6592b81ff961228f0b0da9af85302d4a11`
và ghi nhận gate pass trong [evidence P5](P5.vi.md) §1. CI run `35109453587`
cũng báo P4 pass trên Ubuntu và Windows. Vì vậy dòng `pending` trong snapshot
ban đầu không còn là trạng thái đúng cho revision P4 đó. Đây là xác minh lịch sử
của đúng digest nêu trên, không phải gate mới cho source tree audit hiện tại.

Đã kiểm tra source/evidence tiền nhiệm và P3 gate thật trước P4. Lần đầu P3-S04
fail `started.elapsed() < 5s`; ba lượt riêng và ba lượt toàn target P3 đều pass,
sau đó prerequisite gate đầy đủ pass với digest
`sha256:6ba31afe3d84ac2f0b0db340f1d861398f36d1bd2aa7514dbadf430e8b2492da`.
Không nới assertion thời gian, không sửa code timing P3.

### Audit P0–P7 follow-up — version-specific source lineage

Rà source-version freshness phát hiện `invalidate_memory_source`, retention
archive/invalidate và dependency cascade từng match các row của version cũ. Sửa
production query để freshness và status retention chỉ theo `derived_version` đang
là `memory_assets.current_version`; một correction không bị nguồn/lineage đã
supersede làm stale.

`forget` giữ semantics riêng: vì version cũ vẫn lưu payload đã dẫn xuất, purge
phải quét mọi version của `memory_sources` và `memory_dependencies`, rồi xóa toàn
bộ asset history cùng FTS/lineage trong transaction tombstone+journal. Hồi quy
`p4_source_invalidation_only_uses_the_current_version_sources` và
`p4_source_forget_removes_assets_that_only_match_historical_lineage` pass; toàn
target `phase_p4` hiện **28/28 pass** trên Windows. Đây là test evidence cho source
audit hiện tại, không thay gate/digest P4 lịch sử ở trên. Official P7 gate cuối
đang được chạy lại sau thay đổi shared store query.

## 2. Ownership và contracts

[SPEC](../specs/P4.vi.md) ghi dependencies, failure model và negative constraints.
`harness-memory` sở hữu policy/extraction/retrieval/invalidation. Memory schema
SQLite bổ sung revision **1** gồm assets, immutable versions, grants, bindings,
dependencies, jobs, contiguous cursors, FTS5 và revision/audit, qua single writer/
host fence hiện có. Giữ nguyên event/ID/schema versions cũ. Runtime kiểm tra trước
mỗi provider attempt; chỉ P2 admission/freeze context và ghi memory refs thực sự
được nhận. CLI, registry, verifier và CI chỉ mở rộng đến P4.

Principal/scope flags là local host inputs được tin cậy; không expose trực tiếp
cho model tự điều khiển. Rendering seal riêng phát hiện sửa contribution, không
phải isolation khỏi code bất kỳ trong host process.

## 3. Mapping acceptance chính xác

Required tests nằm ở `crates/harness-cli/tests/phase_p4.rs`, chạy bằng
`cargo test -p harness-cli --test phase_p4 --locked`. Target sau sửa:
**22 passed, 0 failed, 0 ignored**; nghiệm thu gauntlet cuối còn pending.

| Step/case | Exact test |
|---|---|
| S01 | `p4_s01_memory_contracts_and_publication_policy_are_versioned` |
| S02 | `p4_s02_scoped_assets_and_cas_writes_are_atomic` |
| C07 | `p4_c07_three_principals_publish_with_cas_no_lost_update` |
| C08 | `p4_c08_forged_scope_cannot_search_read_or_export` |
| S03 | `p4_s03_durable_extraction_jobs_settle_atomically` |
| C12 | `p4_c12_crash_before_settlement_keeps_job_and_cursor_atomic` |
| C16 | `p4_c16_sequence_cursor_handles_same_timestamp_pages` |
| C17 | `p4_c17_out_of_order_range_cannot_advance_contiguous_cursor` |
| C22 | `p4_c22_missing_changed_or_malformed_extractor_does_not_advance` |
| S04 | `p4_s04_l1_l2_and_confirmed_profile_updates_preserve_sources` |
| C13 | `p4_c13_injected_memory_never_becomes_independent_evidence` |
| S05 | `p4_s05_bounded_fts_retrieval_contributes_through_context_builder` |
| C05 | `p4_c05_optional_extraction_failure_does_not_block_working_state_resume` |
| C26 | `p4_c26_fts_empty_vietnamese_identifiers_and_vector_timeout_are_distinct` |
| S06 | `p4_s06_invalidation_cache_and_budgets_are_enforced` |
| C20 | `p4_c20_revocation_invalidates_transitive_derived_context` |
| C30 | `p4_c30_budget_and_shutdown_pause_jobs_durably` |
| S07 | `p4_s07_cli_catch_up_inspects_memory_and_resume_is_independent` |

Tests bổ sung trong `phase_p4/extended.rs`:

- `extended::p4_runtime_rechecks_revision_before_each_provider_attempt`
- `extended::p4_grant_revocation_hides_derived_search_read_and_export`
- `extended::p4_source_changes_semantic_merge_and_binding_are_versioned`
- `extended::p4_inflight_shutdown_and_each_budget_dimension_pause_without_cursor_advance`

Dùng SQLite/FTS5, SessionService, RuntimeService, ContextBuilder và CLI binary
thật; chỉ giả lập boundaries extractor/vector/provider bên ngoài. C12 kiểm counts
assets/versions/dependencies = 0 khi rollback, từ chối lease cũ sau reopen, sau đó
settle đúng một asset/version/dependency. Đây là pre-commit fault window được inject,
không phải mọi OS/power-loss window. C17 cố ý completion ngoài thứ tự; no-facts
tiến contiguous cursor, fail thì không. C09/C18/C19/C29/K02/K10 giữ tests tiền nhiệm,
thêm lineage/mandatory-context/optional-resume/pre-dispatch, không đổi primary owner.

## 4. Gauntlet tái chạy được

```powershell
pwsh -NoProfile -File scripts/Verify-P4Gauntlet.ps1
```

Toolchain: Windows x86_64 MSVC; PowerShell **7.6.6**; Rust **1.97.1**
(`8bab26f4f 2026-07-14`); Cargo **1.97.1** (`c980f4866 2026-06-30`).
Pin: `rust-toolchain.toml`, `Cargo.lock`.
Raw log local: `.harness/artifacts/p4-gauntlet.log` (ignored, không commit).

| Lớp | Command/component đã lưu trong repo | Kết quả cuối |
|---|---|---|
| Phase + P0–P3 regressions | `Verify-Phase.ps1 -Phase P4 -Json` | pending |
| Format | `cargo fmt --all -- --check` | pending |
| Types/lint | `cargo clippy --workspace --all-targets --locked -- -D warnings` | pending |
| Full suite | `cargo test --workspace --all-targets --locked` | pending |
| Required tests/discovery | exact registry names, nonempty/nonignored | pending |
| Properties | `cargo test -p harness-memory --lib --locked` | pending |
| Manual mutants | `Verify-P4Mutations.ps1` | pending |
| Gate negative controls | `Verify-Phase.ps1 -Phase P4 -SelfTest` | pending final run |
| Docs/negative controls | `Verify-Docs.ps1 -SelfTest` | pending final run |
| CLI demo thật | S07 compiled `ha`, SQLite tạm | pending final run |

Năm mutant sửa CAS, scope, cursor, transitive invalidation và normalization.
Source copies tạm dùng target riêng; mỗi kill phải có runtime receipt duy nhất,
Cargo exit 101 và exact failed test. Phục hồi bytes gốc rồi chạy lại suite. Checker
controls từ chối survivor, compiler failure và thiếu receipt. Chỉ normalization
được chạy riêng bằng property suite; bốn mutant khác kiểm acceptance defenses,
không chứng minh property coverage CAS/scope/cursor.

## 5. Lịch sử RED, lớp bỏ qua và confidence

Đã thấy RED: thiếu contract/stub APIs (yếu hơn behavioral assertions đầy đủ), thiếu
CLI command, Clap required/global sai, binding ID count **0 thay vì 1**, rendering
bị sửa vẫn trả `Ok(())`. Sửa implementation, không nới assertions. Gauntlet trung
gian từ chối formatting sau khi thêm assertions cuối; chỉ lượt post-edit cuối tính
nghiệm thu.

Old-coder **Tier 3**; spec approval: **not obtained (autonomous run)**.
Independent verification: **not performed**, user cấm spawn agents.
Builder cũng viết implementation/tests/checkers; đây là evidence có giới hạn,
không phải chứng nhận độc lập hay proof tuyệt đối.

- Changed-line/branch coverage: **unverified**, thiếu `cargo-llvm-cov`.
- Advisory audit: **unverified**, thiếu `cargo-audit`. Không đổi external package
  versions; direct `unicode-normalization = 0.1.25` đã có trong lock transitively,
  license `MIT OR Apache-2.0`. Links khác tái dùng workspace deps; feature signal
  hiện có của Tokio cho phép hủy CLI.
- Broad secret scan: **unverified**, thiếu `gitleaks`. Scan pattern hẹp
  private-key/GitHub/OpenAI-token không có match; không bảo đảm scan toàn diện.
- Randomized suite order: **chưa chạy**, stable Rust được pin chưa cấu hình shuffle
  runner. Biến thiên timing P3 đã ghi phía trên.
- Network extractors/vectors thật, deployment, physical purge/backup/release
  hardening đầy đủ và P5: **ngoài scope**. FTS là baseline; extractor mặc định
  disabled, mock xác định phải opt-in. Job thiếu extractor giữ blocked inspect
  được; đổi strategy phải chọn replay rõ.
- Token counts là byte-based estimates hiện có, không phải tokenizer model.
- Kế thừa [findings review P0–P3](P0-P3_REVIEW.vi.md): approval identity, SSE
  per-index tool deltas, compaction snapshot CAS, queued process cancellation.
  P4 không chứng nhận full conformance P0–P3.

## 6. Delivery

Commit/push và trạng thái terminal CI ở thời điểm viết bản ghi gốc: **pending**;
follow-up phía trên xác nhận revision P4 đã được gate/CI pass sau đó.
[Restart handoff](../handoffs/P4.vi.md). Không commit credentials, runtime DB hay
private trajectory. Dừng trước P5; quyền implementation không phải deployment.
