# Bàn giao khởi động lại P2

[English](P2.en.md) | Tiếng Việt

Ngày: 10/09/2026
Trạng thái: implementation P2 đã hoàn thành, commit `a34c20ea95f1aeaac67dffc77a7e9cac9ff99234`, push lên `origin/master` và được xác minh remote bởi hai workflow success bên dưới. Dừng trước P3.

## Định danh đã test và tiền nhiệm

- Tiền nhiệm P1 được xác minh tại `origin/master ffffa35cbd6afeabf989d88dde95162cf16e6753`; gate P1: 20 test phát hiện / 18 bắt buộc.
- Regression exact tiền nhiệm P0: pass 8/8.
- Digest source P2 cuối: `sha256:947013b3bc1b88def8fabd867b74805072559efd4be4d58ff097cec132bdbf9c` trên 108 file (metadata trong `docs/evidence/` và `docs/handoffs/` được verifier loại khỏi digest).
- CI remote: [Documentation checks run 34470875361](https://github.com/DEVfancybear/harness-agents/actions/runs/34470875361) và [Rust foundation checks run 34470875366](https://github.com/DEVfancybear/harness-agents/actions/runs/34470875366), cả hai terminal `success` cho implementation SHA ở trên.
- Giữ thay đổi không liên quan trong worktree; không reset hoặc clean repository.

## Step đã hoàn tất và evidence restart

| Step | Evidence khởi động lại |
|---|---|
| P2-S01 | runtime ID/error trong `harness-types`, state transition có version và runtime schema additive. |
| P2-S02 | provider boundary normalize, SSE decoder, DeepSeek adapter và credential resolver trong `crates/harness-providers/`. |
| P2-S03 | admission mandatory-first, ranking, token estimate, overflow explanation và source manifest trong `harness-session::context`. |
| P2-S04 | ordering bền vững composition/context/frozen request/provider attempt và immutable theo config boundary. |
| P2-S05 | compaction runtime, working-state fallback xác định được, resume và decision supersession. |
| P2-S06 | continuation session mới, checkpoint/lineage, offline replay, unknown-critical block và cancel/retry bounded. |
| P2-S07 | CLI keyless `run`, `resume`, `continue`, `context inspect`, `session replay --offline`. |

Acceptance test nằm tại `crates/harness-cli/tests/phase_p2.rs`; registry và verifier yêu cầu toàn bộ P2 cùng C04/C06/C09/C14/C18/C19/K10/K11. Test dùng SQLite, runtime, session, provider và CLI thật; chỉ boundary HTTP/provider bên ngoài dùng fixture cục bộ hoặc MockProvider.

## Quy trình khởi động lại

1. Đọc handoff này, [evidence P2](../evidence/P2.vi.md), [SPEC P2](../specs/P2.vi.md), [runbook P2](../implementation/P2_RUNTIME_CONTEXT.vi.md) và các contract kiến trúc được link.
2. Chạy `git status --short` và `git diff --check`; giữ thay đổi không liên quan.
3. Chạy `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings` và `cargo test --workspace --all-targets --locked`.
4. Chạy `pwsh -NoProfile -File scripts/Verify-Phase.ps1 -Phase P2 -Json` và `pwsh -NoProfile -File scripts/Verify-Docs.ps1 -SelfTest`.
5. Xác nhận `git rev-parse HEAD`, `git ls-remote origin refs/heads/master` và kết luận cuối của GitHub Actions cho đúng SHA trước khi gọi là remote verified.

## Guardrail và gap rõ ràng

- Giữ nguyên ID/error, schema version 1, canonical hash, transaction writer duy nhất, và fixture đã chấp nhận của P0/P1.
- Không thêm tool execution P3, memory extraction P4, scheduling P5, plugin P6, release/deployment P7 hay Web UI P8 trong phase này.
- Chưa dùng credential/API thật, chưa smoke provider bên ngoài, chưa chạy Linux, coverage/mutation, release package, deployment hoặc production activation.
- Unknown critical phải chặn active resume nhưng vẫn inspectable offline; cancel và retry vẫn bounded.

Assignment an toàn tiếp theo chỉ là P3 sau yêu cầu mới rõ ràng và SPEC/gate phase mới. Có thể cập nhật metadata evidence/handoff ở commit sau mà không đổi digest source P2.
