# P1 restart handoff

English | [Tiếng Việt](P1.vi.md)

Date: 2026-09-10
State: P1 source is implemented and locally green; the worktree is intentionally uncommitted and unpublished. Stop before P2.

## Tested identity and current worktree

- Predecessor `HEAD/origin/master`: `bca9ed0413423621bb391880c06ab126c79b0765`.
- Pre-edit P0 evidence digest: `sha256:0edcf7d59d8c409ad1a76f9c4f88c92ec2a8e50c06baeb7ea4f187ccb3be7dc3`.
- P1 final implementation digest (excluding P1 evidence/handoff metadata): `sha256:5ddf6b6b3624baf3492c6dc70e14bcc4228b9acaa5f0deb38cd7c64420706ce9` across 96 non-metadata source paths.
- Post-metadata final P0 gate: passed; both phase gates were rerun after the evidence/handoff files were added. The P0 digest is emitted by its verifier but is not copied into a file that P0 hashes, avoiding a self-referential value.
- `git status --short` is expected to show P1-owned modified files plus new `harness-kernel`, `harness-session`, `harness-store-sqlite`, P1 tests, SPEC, evidence, and handoff paths. Do not reset or clean them.

## Completed steps and key files

| Step | Restart evidence |
|---|---|
| P1-S01 | `docs/specs/P1.{en,vi}.md`, typed IDs/errors, and public store/session/kernel APIs. |
| P1-S02 | `crates/harness-store-sqlite/` migration/lock/fence/lease implementation and real diagnostics/fault tests. |
| P1-S03 | `crates/harness-kernel/` graph, scope, resource, provider lease, and shutdown implementation with K01–K07. |
| P1-S04 | `crates/harness-session/` atomic admission, inbox/ledger/projection/marker commit, idempotency, and raw text preservation. |
| P1-S05 | Durable artifact publish, snapshot hash/coverage validation, journal-tail recovery, corruption and unknown-critical handling. |
| P1-S06 | `crates/harness-cli/src/bin/p1_fixture_host.rs` and real child terminate/reopen tests for C01/C02/C15/C21/C24. |
| P1-S07 | CLI metadata adapters, registry entries, verifier P1 support, and bilingual evidence/handoff. |

## Restart procedure

1. Read this handoff, [P1 evidence](../evidence/P1.en.md), the [P1 SPEC](../specs/P1.en.md), and the linked architecture contracts before editing.
2. Run `git status --short` and `git diff --check`; preserve all P1-owned and unrelated user changes.
3. Run `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --locked -- -D warnings`.
4. Run `cargo test --workspace --all-targets --locked`, `cargo test -p harness-cli --test phase_p0 --locked -- --test-threads=1`, and `cargo test -p harness-cli --test phase_p1 --locked -- --test-threads=1`.
5. Run `pwsh -NoProfile -File scripts/Verify-Phase.ps1 -Phase P1 -SelfTest`, `pwsh -NoProfile -File scripts/Verify-Phase.ps1 -Phase P1 -Json`, and `pwsh -NoProfile -File scripts/Verify-Phase.ps1 -Phase P0 -Json`.
6. Run `pwsh -NoProfile -File scripts/Verify-Docs.ps1 -SelfTest`. If a check fails, repair only the owning P1/documentation path and repeat the relevant gates; do not bypass a guard or begin P2.

## Contract guardrails and known gaps

- Keep P0 schema version 1, ID formats, authority labels, canonical hash rules, and existing fixture meanings unchanged. Any incompatible change needs a new reviewed SPEC/ADR.
- Keep the SQLite transaction coordinator as the sole durable writer. Do not add a second store authority, acknowledge before commit, or treat observer notification as persistence.
- Synthetic receipts do not prove P3 tool execution. P1 has no model calls, shell/process side effects, memory extraction, Tencent adapter, multi-agent scheduling, external plugin process, Web UI, backup/migration upgrade flow, or production activation.
- Fault plans are deterministic test boundaries, not a physical disk-full guarantee. Linux and remote CI have not been run on this unpushed revision.

## Publication and next safe assignment

No commit or push was made because the P1 assignment explicitly forbade it. A later publication request must stage only task-owned paths, create a narrow commit, push, and verify the actual remote CI conclusion. Until then, this worktree is the source of truth. The next safe assignment is user acceptance of P1; only after that may a separate P2 SPEC and implementation request be considered.
