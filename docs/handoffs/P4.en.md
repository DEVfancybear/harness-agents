# P4 restart handoff

English | [Tiếng Việt](P4.vi.md)

## P0–P7 code-audit follow-up — 2026-09-23

- Source invalidation and descendant traversal now follow current-version
  lineage; stale source/dependency rows cannot invalidate or archive corrected
  current memory.
- `forget` walks every historical version and removes any asset retaining an old
  copy of the forgotten source. Status, FTS, lineage, tombstone and journal are
  committed atomically.
- `cargo test -p harness-cli --test phase_p4 --locked`: **28/28 passed**. Main
  regressions: `p4_source_invalidation_only_uses_the_current_version_sources`
  and `p4_source_forget_removes_assets_that_only_match_historical_lineage`.
- This result belongs to the P0–P7 audit worktree; it does not change reviewer-
  owned P4 status or the historical digest. The final P7 gate must run on the
  current source tree.

## 1. Boundary

Original checkpoint: P4-S01–S07 were implemented, and acceptance was not finalized
when that handoff was written. P5 later reran the P4 gate and CI verified that P4
revision; see the follow-up in [P4 evidence](../evidence/P4.en.md). Base includes
preserved P0–P3 review `a76b89d`.
Read [SPEC](../specs/P4.en.md) and [evidence](../evidence/P4.en.md).

## 2. Verified and pending

Post-fix `cargo test -p harness-cli --test phase_p4 --locked`: **22 passed,
0 failed, 0 ignored**. Binding ID round-trip and tampered contribution regressions
were observed RED and now pass. P5 later reran `Verify-Phase.ps1 -Phase P4 -Json`
with digest `sha256:d78de82effebe4d06a5ab47f2619ea6592b81ff961228f0b0da9af85302d4a11`;
CI run `35109453587` passed P4 on Ubuntu and Windows. This handoff's pending state
has been superseded; the targeted 22/22 result alone was not treated as sufficient.

## 3. Original continuation (superseded)

1. Inspect `git status --short`, `git log -1`, remote SHA and running verifier.
2. If no verifier is active, run:
   `pwsh -NoProfile -File scripts/Verify-P4Gauntlet.ps1`.
   It runs receipt-proven manual mutations/restored suites, P4 gate and P0–P3 regressions.
3. Record final JSON digest/counts, then verify docs after evidence-only edits:
   `pwsh -NoProfile -File scripts/Verify-Docs.ps1 -SelfTest`.
4. Check actual Actions conclusions with `gh run list --commit <pushed-sha>` and
   `gh run view <run-id>`; CI matrix includes P0/P3/P4 on Ubuntu and Windows.
5. Commit/push scoped evidence/handoff updates after verification. Do not call the
   phase complete if mandatory gates fail or required platform checks are missing.
6. Stop before P5; a new assignment is required.

Memory schema revision 1 is additive and uses the existing coordinator.
Never commit runtime DB/private trajectories. Coverage/advisory scan and inherited
P0–P3 review gaps are explicitly disclosed in evidence, not silently considered pass.
