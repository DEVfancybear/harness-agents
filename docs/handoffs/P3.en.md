# P3 restart handoff — coding tools and execution safety

English | [Tiếng Việt](P3.vi.md)

Date: 2026-09-10
Implementation state: P3-S01 through P3-S07 are complete in commits `6666cbd` and `f0a4ac8`, with local phase gate PASS. Remote Rust foundation run [34495805908](https://github.com/DEVfancybear/harness-agents/actions/runs/34495805908) passed P0/P3 on Ubuntu and Windows; Documentation run [34495334678](https://github.com/DEVfancybear/harness-agents/actions/runs/34495334678) passed both jobs. This handoff is the restart boundary; the next phase is not started.

## Tested identity and predecessor

- Baseline before P3: clean `HEAD / origin/master 2f9379b1bf2ae64f213606762d66cc448b469610`.
- P2 predecessor: `a34c20ea95f1aeaac67dffc77a7e9cac9ff99234`, source digest `sha256:947013b3bc1b88def8fabd867b74805072559efd4be4d58ff097cec132bdbf9c`.
- Final P3 source digest: `sha256:5b1298e8a0f2602f8468c2b65379448669d93fe7e1e19d5a66ab46df6e797f1a` across 123 files. Phase evidence/handoff metadata is excluded by the verifier.
- Local gate: P0 8/8, P1 18 required (20 discovered), P2 15 required (16 discovered), P3 15/15; format, clippy, workspace tests, and docs checks all passed.
- Remote CI is verified for implementation SHA `f0a4ac882660fd7d28317f648f407036a944d32f` by the successful run above; no deployment or activation is implied. The later metadata-only commits do not change the source digest.

## Completed steps and restart evidence

| Step | Restart evidence |
|---|---|
| P3-S01 | `harness-tools::contracts`: nine versioned actions/schemas, canonical hashes, typed approval binding, immutable receipt/view model. |
| P3-S02 | `harness-tools::service::ToolExecutionService`: the sole validate/transform/approval/intent/dispatch/settlement gate; observer notification is post-receipt. |
| P3-S03 | `harness-tools::workspace`: canonical rooted paths, link/reparse denial, `.gitignore` walk, UTF-8/binary checks, CRLF/Unicode atomic patch, hashes and redaction. |
| P3-S04 | `harness-tools::process`: structured argv and explicit shell actions, bounded output, timeout/cancel, Windows Job Object or POSIX session cleanup; strict isolation is an explicit denial. |
| P3-S05 | Git identity/fingerprint and reassociation in `workspace`/store; task update is projection-only; artifact scope and redaction are durable. |
| P3-S06 | Additive tools schema and atomic store commits/fault points; pending/unknown recovery and no-blind-rerun reconciliation. |
| P3-S07 | CLI `code capabilities` and keyless `code fixture`, P2 `CodingLoopService`, acceptance registry, phase gate, CI matrix, and bilingual SPEC/evidence. |

Acceptance tests are in `crates/harness-cli/tests/phase_p3.rs`; the registry requires C03, C10, C23, C29, K08, K09 and all P3 steps. They use real temporary SQLite/files/Git/subprocess/runtime components. Only the external provider boundary in the deterministic CLI fixture uses `MockProvider`.

## Restart procedure

1. Read this handoff, [P3 evidence](../evidence/P3.en.md), [P3 SPEC](../specs/P3.en.md), [P3 runbook](../implementation/P3_CODING_TOOLS.en.md), and the linked contracts.
2. Run `git status --short` and `git diff --check`; preserve unrelated worktree changes. Never reset or clean the repository as a restart shortcut.
3. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, and `cargo test --workspace --all-targets --locked`.
4. Run `pwsh -NoProfile -File scripts/Verify-Docs.ps1 -SelfTest` and `pwsh -NoProfile -File scripts/Verify-Phase.ps1 -Phase P3 -Json`.
5. Build and inspect the keyless capability boundary with `cargo build -p harness-cli --bin ha --locked` and `target\debug\ha.exe code capabilities --json` (PowerShell path syntax may vary).
6. Confirm `git rev-parse HEAD`, `git ls-remote origin refs/heads/master`, and the terminal GitHub Actions conclusion for the pushed SHA. The implementation SHA `f0a4ac8` is covered by the successful run linked above; a green local gate is not proof of production activation.
7. For a pending tool after restart, inspect the durable intent and call `ToolExecutionService::reconcile_pending` only after verifying the workspace post-state. It never dispatches a stored action again; non-exact outcomes stay `outcome_unknown` for user-directed resolution.

## Guardrails and explicit gaps

- Preserve P0/P1/P2 schema version 1, identifiers, canonical hashes, single-writer transaction boundary, task/session ownership, and frozen P2 requests.
- Approval is single-use and binds actor, canonical action, workspace root/fingerprint, policy revision, and tool revision. Parent/ancestor deny remains monotonic; strict isolation remains denied on this host.
- Do not add P4 memory extraction, P5 multi-agent scheduling/delegation, P6 plugins, P7 release/deployment, or P8 Web UI under this phase.
- No live provider credential, external network smoke, Linux local run, coverage/mutation/audit, release package, deployment, or production activation was performed. These are explicit gaps, not passing claims.
- The first RED process run exposed a Windows lifecycle race, and the first CI run exposed a Unix-only precision lint in a test helper. The final runner uses one terminal wait, bounded kill/reap, and a host mutex; the helper uses integer duration formatting. Keep these portability/lifecycle contracts when extending tools.

The next safe action is a separately authorized P4 phase. Stop here until that assignment, SPEC, predecessor evidence, and gate are provided.
