# Restart handoff P4

[English](P4.en.md) | Tiếng Việt

## Follow-up code audit P0–P7 — 23/09/2026

- Sửa source invalidation và descendant traversal chỉ theo current version lineage;
  stale source/dependency rows không còn làm invalidated/archived asset hiện hành.
- `forget` đi qua mọi version lịch sử để xóa toàn bộ asset vẫn giữ bản cũ chứa
  source bị quên; status, FTS, lineage, tombstone và journal nằm trong transaction.
- `cargo test -p harness-cli --test phase_p4 --locked`: **28/28 pass**. Regression
  chính: `p4_source_invalidation_only_uses_the_current_version_sources` và
  `p4_source_forget_removes_assets_that_only_match_historical_lineage`.
- Kết quả này thuộc worktree audit P0–P7; không đổi trạng thái reviewer-owned P4
  hay digest lịch sử. Gate P7 cuối cần chạy lại trên source tree hiện tại.

## 1. Ranh giới

Checkpoint gốc: đã triển khai P4-S01–S07 và chưa chốt nghiệm thu khi handoff được
viết. P5 sau đó rerun gate P4 và CI xác nhận revision P4; xem follow-up trong
[evidence P4](../evidence/P4.vi.md). Base giữ nguyên review P0–P3 `a76b89d`.
Đọc [SPEC](../specs/P4.vi.md) và [evidence](../evidence/P4.vi.md).

## 2. Đã kiểm tra và còn chờ

Sau sửa, `cargo test -p harness-cli --test phase_p4 --locked`: **22 passed,
0 failed, 0 ignored**. Đã thấy RED rồi GREEN cho binding ID round-trip và
contribution bị sửa. P5 sau đó chạy lại `Verify-Phase.ps1 -Phase P4 -Json` và
ghi digest `sha256:d78de82effebe4d06a5ab47f2619ea6592b81ff961228f0b0da9af85302d4a11`;
CI run `35109453587` pass P4 trên Ubuntu và Windows. Trạng thái pending của
checkpoint này đã được supersede; không suy từ targeted 22/22 đơn lẻ.

## 3. Tiếp tục tại checkpoint gốc (đã supersede)

1. Kiểm `git status --short`, `git log -1`, remote SHA và verifier đang chạy.
2. Nếu verifier không còn chạy, chạy:
   `pwsh -NoProfile -File scripts/Verify-P4Gauntlet.ps1`.
   Lệnh chạy manual mutants có receipts/restored suites, P4 gate và regressions P0–P3.
3. Ghi JSON digest/counts cuối; sau khi chỉ sửa evidence, kiểm docs:
   `pwsh -NoProfile -File scripts/Verify-Docs.ps1 -SelfTest`.
4. Kiểm Actions thực tế bằng `gh run list --commit <pushed-sha>` và
   `gh run view <run-id>`; CI matrix gồm P0/P3/P4 trên Ubuntu và Windows.
5. Commit/push riêng evidence/handoff sau kiểm chứng. Không nhận phase hoàn tất
   nếu gate bắt buộc fail hoặc thiếu required platform checks.
6. Dừng trước P5, cần assignment mới.

Memory schema revision 1 là bổ sung, dùng coordinator hiện có. Không commit runtime
DB/private trajectories. Coverage/advisory scan và gaps review P0–P3 kế thừa đã ghi
rõ trong evidence, không coi ngầm là pass.
