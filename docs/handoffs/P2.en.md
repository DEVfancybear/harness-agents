# P2 restart handoff

English | [Tiếng Việt](P2.vi.md)

Date: 2026-09-10
State: P2 implementation is complete, committed as `a34c20ea95f1aeaac67dffc77a7e9cac9ff99234`, pushed to `origin/master`, and remotely verified by the two successful workflows linked below. Stop before P3.

## Tested identity and predecessor

- P1 predecessor verified at `origin/master ffffa35cbd6afeabf989d88dde95162cf16e6753`; P1 gate: 20 discovered / 18 required.
- P0 exact predecessor regression: 8/8 passed.
- Final P2 source digest: `sha256:947013b3bc1b88def8fabd867b74805072559efd4be4d58ff097cec132bdbf9c` across 108 files (phase metadata under `docs/evidence/` and `docs/handoffs/` is excluded by the verifier).
- Remote CI: [Documentation checks run 34470875361](https://github.com/DEVfancybear/harness-agents/actions/runs/34470875361) and [Rust foundation checks run 34470875366](https://github.com/DEVfancybear/harness-agents/actions/runs/34470875366), both terminal `success` for the implementation SHA above.
- Keep unrelated worktree changes. Do not reset or clean the repository.

## Completed steps and restart evidence

| Step | Restart evidence |
|---|---|
| P2-S01 | `harness-types` runtime IDs/errors, versioned runtime state transitions, additive runtime schema. |
| P2-S02 | `crates/harness-providers/` normalized provider boundary, SSE decoder, DeepSeek adapter and credential resolver. |
| P2-S03 | `harness-session::context` mandatory-first admission, ranking, token estimates, overflow explanation and source manifest. |
| P2-S04 | Durable composition/context/frozen-request/provider-attempt ordering and config-boundary immutability. |
| P2-S05 | Runtime compaction, deterministic working-state fallback, resume and decision supersession. |
| P2-S06 | New-session continuation, lineage/checkpoint reuse, offline replay, unknown-critical blocking and bounded cancel/retry. |
| P2-S07 | Keyless CLI `run`, `resume`, `continue`, `context inspect`, and `session replay --offline`. |

Acceptance tests are in `crates/harness-cli/tests/phase_p2.rs`; the registry and phase verifier require all P2 steps plus C04/C06/C09/C14/C18/C19/K10/K11. Tests use real SQLite, runtime, session, provider and CLI components; only external HTTP/provider boundaries use local fixtures or MockProvider.

## Restart procedure

1. Read this handoff, [P2 evidence](../evidence/P2.en.md), [P2 SPEC](../specs/P2.en.md), [P2 runbook](../implementation/P2_RUNTIME_CONTEXT.en.md), and linked architecture contracts.
2. Run `git status --short` and `git diff --check`; preserve unrelated changes.
3. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, and `cargo test --workspace --all-targets --locked`.
4. Run `pwsh -NoProfile -File scripts/Verify-Phase.ps1 -Phase P2 -Json` and `pwsh -NoProfile -File scripts/Verify-Docs.ps1 -SelfTest`.
5. Confirm `git rev-parse HEAD`, `git ls-remote origin refs/heads/master`, and the terminal GitHub Actions conclusion for the pushed SHA before calling the work remotely verified.

## Guardrails and explicit gaps

- Preserve P0/P1 IDs, error strings, schema version 1, canonical hashes, transaction-writer ownership, and accepted fixtures.
- Do not add P3 tool execution, P4 memory extraction, P5 scheduling, P6 plugins, P7 release/deployment, or P8 Web UI under this phase.
- No live credential/API, external provider smoke, Linux run, coverage/mutation, release package, deployment, or production activation was performed.
- Unknown critical events must block active resume while remaining inspectable offline; cancellation and retry remain bounded.

The next safe assignment is P3 only after an explicit new request and a fresh phase SPEC/gate. The evidence/handoff metadata may be updated in a later commit without changing the P2 source digest.
