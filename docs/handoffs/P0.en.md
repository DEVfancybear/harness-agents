# P0 restart handoff

English | [Tiếng Việt](P0.vi.md)

Date: 2026-09-10
State: P0 local gate passed; await user acceptance and do not begin P1.

## Tested source identity

- Baseline before P0: `7a554b400a1c0936266bff5f730cd774befc664d`.
- Tested implementation tree: `sha256:1d0461b9595def0257bc8a357f9611991bedc9819f2c5c2a6f8af888264449de` across 78 files. The digest excludes only `docs/evidence/P0.{en,vi}.md` and `docs/handoffs/P0.{en,vi}.md`; this avoids self-reference while retaining all implementation and governing documentation.
- Key fingerprints: `Cargo.toml` `0c1485a91914dc2ea543b19f07415ed2fa707683979081747f94f665e686107b`; `Cargo.lock` `1dfff85a82cded2ef06d87aa631877872783816fbeb5ffbed6a85b7154e041c6`; `crates/harness-cli/src/main.rs` `6cbb6487145d0119ff64b9ae99137035d4faf832bb2bf1f10ec7379f54110298`; `crates/harness-types/src/contracts.rs` `1e0ba66cb064f42e42690871c436f12b43d5e584d4841788d1de52fd1d50e37b`; `schemas/event-envelope.v1.schema.json` `96297839a548b486bba8c822c27b5cba6ca94609129b22529ce85c981cd34ee6`; `tests/acceptance/registry.json` `1f7a531ea7a8db5dda07a734bf444b6b4d0e2dc27b1db38da18da90442dd0f0f`; `scripts/Verify-Phase.ps1` `cfa1faeebe79d671978504a4888989eecb58fb9b7dea4414b741786dfca6a4a6`.

## Completed phase steps

| Step | Completion evidence |
|---|---|
| P0-S01 | Bilingual SPEC, accepted boundaries, baseline/toolchain/dependency inventory. |
| P0-S02 | Pinned workspace and real minimal `ha` CLI. |
| P0-S03 | Typed contracts, strict validation, generated schemas, and schema drift test. |
| P0-S04 | Static continuation fixture plus independent integrity verifier and negative artifacts. |
| P0-S05 | Acceptance registry, eight real acceptance tests, fail-closed runner, and self-test. |
| P0-S06 | Fresh local gate passed; Windows/Linux CI declared but remote CI unverified. |
| P0-S07 | ADR, bilingual evidence, this restart handoff, source digest, and mutation evidence. |

## What exists

- Workspace: `Cargo.toml`, `Cargo.lock`, `rust-toolchain.toml`, `.gitignore`.
- Shared contracts: `crates/harness-types/` owns IDs, typed errors, contracts, canonical hashing, schemas, and static fixture verification.
- CLI: `crates/harness-cli/` exposes only `ha --help`, `ha --version`, and `ha config validate`.
- Contract assets: `schemas/`, `tests/fixtures/p0/`, and `tests/acceptance/registry.json`.
- Verification: `scripts/Verify-Phase.ps1`, `crates/harness-cli/tests/phase_p0.rs`, and `.github/workflows/ci.yml`.
- Decision/spec/evidence: [ADR-0001](../adr/ADR-0001-P0-FOUNDATION-BOUNDARIES.en.md), [P0 SPEC](../specs/P0.en.md), and [P0 evidence](../evidence/P0.en.md).

## Restart procedure

1. Confirm the checkout still has only the P0-owned changes and no unreviewed user edits: `git status --short` and `git diff --check`.
2. Read the P0 SPEC, this handoff, and evidence before changing source.
3. Run `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --locked -- -D warnings`.
4. Run `cargo test --workspace --locked`, then `cargo run -p harness-types --bin generate_schemas --locked` and verify only the nine expected schema files change, if any.
5. Run `pwsh -NoProfile -File scripts/Verify-Phase.ps1 -Phase P0 -SelfTest` and `pwsh -NoProfile -File scripts/Verify-Phase.ps1 -Phase P0 -Json`.
6. Run `pwsh -NoProfile -File scripts/Verify-Docs.ps1 -SelfTest`. If any check fails, repair only P0-owned paths and repeat the gate; do not start P1 as a workaround.

## Contract and scope guardrails

- Do not silently change `schema_version = 1`, ID format, authority labels, or canonical-hash number rules. Any incompatible change needs a new reviewed revision/ADR.
- Do not add a store, SQLite, provider/model request, tool execution, memory extractor, child-agent runner, Web API/UI, secret handling, or release work under P0.
- The static fixture proves fixture integrity, not live restart/recovery. C01–C30 and K01–K14 remain unimplemented.
- Keep test doubles limited to the external Cargo child boundary inside the gate self-test; contract, CLI, fixture, and gate acceptance tests use real components.

## External-state and publication boundary

No commit, push, release, publish, database mutation, provider call, credential, or network service configuration occurred. CI is declared but has not run remotely. A future owner may commit/push only after a separate instruction; then they must verify the remote CI conclusion before claiming CI evidence.

## Next safe assignment

P0 evidence is locally passed and now awaits user acceptance. The next possible phase is P1 kernel/session/SQLite, but it is deliberately not started and requires an explicit user assignment plus its own phase SPEC.
