# Bàn giao khởi động lại P1

[English](P1.en.md) | Tiếng Việt

Ngày: 10/09/2026
Trạng thái: source P1 đã triển khai, xanh cục bộ, commit `051617b37a40a424165c132fca5c595924beb677` và push lên `origin/master`; đang chờ user nghiệm thu. Chưa deployment hoặc làm P2. Dừng trước P2.

## Định danh đã test và worktree hiện tại

- Predecessor `HEAD/origin/master`: `bca9ed0413423621bb391880c06ab126c79b0765`.
- Digest evidence P0 trước khi sửa: `sha256:0edcf7d59d8c409ad1a76f9c4f88c92ec2a8e50c06baeb7ea4f187ccb3be7dc3`.
- Digest implementation P1 cuối (loại metadata evidence/handoff P1): `sha256:5ddf6b6b3624baf3492c6dc70e14bcc4228b9acaa5f0deb38cd7c64420706ce9` trên 96 source path không phải metadata.
- Gate P0 cuối sau metadata: pass; cả hai phase gate đã chạy lại sau khi thêm evidence/handoff. Verifier P0 vẫn phát digest nhưng không chép vào file mà P0 hash, để tránh giá trị tự tham chiếu.
- Xác minh remote cho implementation commit: [Documentation checks run 34465859237](https://github.com/DEVfancybear/harness-agents/actions/runs/34465859237) và [Rust foundation checks run 34465859340](https://github.com/DEVfancybear/harness-agents/actions/runs/34465859340) đều kết thúc `success`.
- Sau commit publish, `git status --short` sạch. Nếu checkout tương lai xuất hiện thay đổi user không liên quan, phải giữ nguyên; không reset hoặc clean.

## Step đã hoàn tất và file chính

| Step | Bằng chứng khởi động lại |
|---|---|
| P1-S01 | `docs/specs/P1.{en,vi}.md`, typed IDs/errors và public API store/session/kernel. |
| P1-S02 | Implementation migration/lock/fence/lease/diagnostics/fault thật trong `crates/harness-store-sqlite/`. |
| P1-S03 | Graph, scope, resource, provider lease, shutdown trong `crates/harness-kernel/`, cùng K01–K07. |
| P1-S04 | Admission atomic, commit inbox/ledger/projection/marker, idempotency và raw text trong `crates/harness-session/`. |
| P1-S05 | Artifact publish bền vững, snapshot hash/coverage, journal-tail recovery, corruption và unknown-critical handling. |
| P1-S06 | `crates/harness-cli/src/bin/p1_fixture_host.rs` và child terminate/reopen thật cho C01/C02/C15/C21/C24. |
| P1-S07 | CLI metadata adapter, registry, verifier P1 và evidence/handoff song ngữ. |

## Quy trình khởi động lại

1. Đọc handoff này, [evidence P1](../evidence/P1.vi.md), [SPEC P1](../specs/P1.vi.md) và các contract kiến trúc được link trước khi sửa.
2. Chạy `git status --short` và `git diff --check`; giữ nguyên mọi thay đổi P1-owned và thay đổi user không liên quan.
3. Chạy `cargo fmt --all -- --check` và `cargo clippy --workspace --all-targets --locked -- -D warnings`.
4. Chạy `cargo test --workspace --all-targets --locked`, `cargo test -p harness-cli --test phase_p0 --locked -- --test-threads=1` và `cargo test -p harness-cli --test phase_p1 --locked -- --test-threads=1`.
5. Chạy `pwsh -NoProfile -File scripts/Verify-Phase.ps1 -Phase P1 -SelfTest`, `pwsh -NoProfile -File scripts/Verify-Phase.ps1 -Phase P1 -Json` và `pwsh -NoProfile -File scripts/Verify-Phase.ps1 -Phase P0 -Json`.
6. Chạy `pwsh -NoProfile -File scripts/Verify-Docs.ps1 -SelfTest`. Nếu fail, chỉ sửa path P1/documentation sở hữu rồi chạy lại gate liên quan; không bypass guard hoặc bắt đầu P2.

## Guardrail contract và khoảng trống đã biết

- Giữ nguyên schema P0 version 1, ID format, authority label, canonical hash rule và nghĩa fixture cũ. Thay đổi không tương thích cần SPEC/ADR mới được review.
- SQLite transaction coordinator là durable writer duy nhất. Không thêm store authority thứ hai, ACK trước commit hoặc coi observer notification là persistence.
- Synthetic receipt không chứng minh tool execution P3. P1 chưa có model call, shell/process side effect, memory extraction, Tencent adapter, multi-agent scheduling, external plugin process, Web UI, backup/schema upgrade flow hay production activation.
- Fault plan là boundary test xác định, không phải bảo đảm hết disk vật lý. Chưa chạy Linux; remote CI của implementation commit đã pass theo link bên trên.

## Publication và assignment an toàn kế tiếp

Implementation P1 thuộc task đã commit `051617b37a40a424165c132fca5c595924beb677`, push lên `origin/master` và được xác minh bởi hai workflow remote success ở trên. Việc publish này không activate hoặc deploy runtime. Assignment an toàn tiếp theo là user nghiệm thu P1; chỉ sau đó mới xem xét SPEC và yêu cầu triển khai P2 riêng.
